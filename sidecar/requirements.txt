# Sidecar-only dependencies -- NOT installed in the prod search-api image
# (see sidecar/Dockerfile.dev). Kept separate from search-api/requirements.txt
# so prod stays completely sidecar-blind, per docs/sidecar-augmentation.md.

# duckdb: point-in-polygon queries against Overture Maps' open Divisions
# dataset (overture_geocode.py), via its spatial and httpfs extensions
# (loaded at runtime with INSTALL/LOAD, not separate pip packages).
duckdb==1.5.2

# Pillow: resizing photos client-side before sending to gpu-ml's
# object-detection inference service (object_detect.py) -- see that file's
# docstring for the sizing rationale.
Pillow==11.0.0
